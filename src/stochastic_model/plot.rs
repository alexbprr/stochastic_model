
#[derive(Default, Clone)]
pub struct PlotInfo {
    pub title: String,
    pub xlabel: String,
    pub ylabel: String,
    pub labels: Vec<String>,
    pub colors: Vec<String>,
    pub values: Vec<Vec<f64>>,
    pub x: Vec<f64>,
    pub y: Vec<f64>,
}

impl PlotInfo {

}